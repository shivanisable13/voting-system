automate build and packaging of ema
automatic build in CI/CD pipeline

folder- employee-management

inside folder
.GitHub
   workflows
      docker-build.yml(txt)
dockerfile
index.html

git init
git add .
git commit -m "Initial commit"
git branch -M main

git remote add origin "GitHub reporistory"
git push -u origin main

ndex.html
<!DOCTYPE html>
<html>
<head>
    <title>Employee Management</title>
</head>
<body>
    <h1>Employee Management Application</h1>
    <p>Application running successfully!</p>
</body>
</html>

dokerfile
FROM nginx:alpine

COPY index.html /usr/share/nginx/html/index.html




  docker-build.yml(txt)
name: Employee App CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout source code
      uses: actions/checkout@v3

    - name: Build Docker Image
      run: docker build -t employee-app .
